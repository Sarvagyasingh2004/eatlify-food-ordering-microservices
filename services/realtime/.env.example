PORT=5004

# Verifies the socket handshake token
JWT_SECRET=

# Guards POST /api/v1/internal/emit - service-to-service only, never shipped
# to the browser
INTERNAL_SERVICE_KEY=

# Socket.IO CORS, comma-separated
CORS_ORIGIN=http://localhost:5173
